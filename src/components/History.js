//This history/createBrowserHistory was created automatically by react-router-dom.
//We are creating this history because instead of giving the reac-router to use its histroy object
///we are going to create the history object for the working of programmatic navigation , in other words
//we need history router object in action creater so that we can automatically navigate to the required file.
import createHistory from "history/createBrowserHistory";

export default createHistory();
